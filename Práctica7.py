from numpy import array
import numpy as np

lista = (["hola", 24, True, "si", 24])

listaNp = np.array([5,6,7,8,9])

tupla = tuple(lista)
tupla2 = tuple(listaNp)

conjunto = set(lista)
conjunto2 = set(listaNp)

D ={
        "nombre": "Pedro",
         "edad": 30,
         "ciudad": "Zacatecas",
         "cash" : 0,
         "vivo": False,
}

